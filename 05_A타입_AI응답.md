# 05. `app/A/style-prompt.ts` — A 타입 AI 응답 스타일

**📁 파일 경로:** `app/A/style-prompt.ts`

---

## 뭐 하는 파일인가요?

AI(ChatGPT)한테 *"A 타입 화면에 어울리게 답해줘"* 라고 시키는 **지침서**.

A 타입 페이지가 챗 메시지를 보낼 때 *이 텍스트를 함께 OpenAI한테 전송*해요.
AI는 이 지침을 보고 그에 맞춰 응답을 만들어 줘요.

---

## 왜 이게 필요한가요?

같은 AI라도 *"어떻게 답해"* 라고 알려주는 방식에 따라 응답이 달라져요.

- A 타입은 *미니멀 텍스트형* → 짧고 라벨드 섹션
- B 타입은 *시각 라벨형* → chip 위주
- C 타입은 *카드 구조형* → card 위주
- D 타입은 *동적 인터랙션형* → 변화 강조

각 타입 폴더에 별도 `style-prompt.ts`가 있어요. AI 응답 스타일이 타입마다 달라야 하니까요.

---

## 언제 만지나요?

✅ **자주 만질 곳**
- AI 응답이 너무 길면 → "더 짧게 답해" 추가
- AI가 자꾸 인라인으로 "1. 2." 나열한다 → 그러지 말라고 명시
- AI 톤이 마음에 안 든다 → 톤 가이드 수정
- 라벨드 섹션을 더 적극 쓰게 하고 싶다 → 예시 추가

---

## 안에 뭐가 있나

```tsx
export const A_TYPE_PROMPT = `
[A 타입 — 미니멀 텍스트형 응답 스타일]
...
[본문(text) 작성 규칙]
...
[A 타입에서 피할 것]
...
[톤]
...
`;
```

- 단 하나의 *큰 텍스트 상수* (`A_TYPE_PROMPT`)
- 백틱(\`) 사이에 자유롭게 한국어로 적힌 지침
- 섹션이 4~5개 (응답 스타일 / 본문 규칙 / 피할 것 / 톤 등)

---

## 안전하게 수정하는 법

### ✅ 마음껏 수정해도 되는 부분

백틱 사이의 *텍스트 부분*은 자유롭게 수정 가능:

```tsx
export const A_TYPE_PROMPT = `
[A 타입 — 미니멀 텍스트형 응답 스타일]
...

[본문(text) 작성 규칙]
- text 필드는 짧은 안내(1~2문장)만 담으세요.   ← 이런 줄을 자유롭게 수정
- ...
`;
```

한 줄 추가하거나, 단어 바꾸기, 예시 더 풀어 쓰기 등 다 OK.

### ❌ 만지면 안 되는 부분

```tsx
export const A_TYPE_PROMPT = `   ← 이 줄 그대로
...
`;                                 ← 이 줄 그대로
```

- `export const A_TYPE_PROMPT =` 부분
- 백틱 두 개 (시작 \`와 끝 \`)
- 이게 깨지면 TypeScript 문법 오류 → 페이지가 안 떠요.

---

## 수정 예시

### 예시 1: AI가 너무 친절하게 말한다 → 톤 더 담백하게

```tsx
[톤]
- 담백하고 짧게.
- 칭찬·과장된 친절 표현 자제.   ← 이 줄 추가
- 매번 응답에 추천 문장 + 판단 근거 + 다음 선택지 중 최소 3가지가 들어가되,
  그것들이 sections로 시각적으로 분리되어 있어야 합니다.
```

### 예시 2: 응답이 너무 길다 → 길이 제한 추가

```tsx
[본문(text) 작성 규칙]
- text 필드는 짧은 안내(1~2문장)만 담으세요.
- 각 sections의 content도 한 단락은 2~3문장 이내로.   ← 이 줄 추가
- ...
```

---

## B/C/D 타입 만들 때

A 폴더 통째로 복사 후:
1. `app/B/style-prompt.ts` 안의 `A_TYPE_PROMPT` → `B_TYPE_PROMPT`로 이름 변경
2. 텍스트 내용을 B 타입 원칙에 맞게 수정 (예: chip 위주 응답)
3. `app/B/page.tsx`에서 import도 `B_TYPE_PROMPT`로 수정
