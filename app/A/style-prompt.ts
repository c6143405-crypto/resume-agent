// A 타입(미니멀 텍스트형) 응답 스타일 가이드.
// app/A/page.tsx 가 API 호출 시 payload.typeStylePrompt 로 함께 전송한다.
// 공통 시스템 프롬프트(=app/api/chat/route.ts 의 SYSTEM_PROMPT)는 Agent 정체성·
// CM1/CM2 의미·userIntent 규칙·금지 사항·JSON 출력 스키마만 담고,
// '응답을 어떤 형태로 보일지'는 이 파일이 책임진다.

export const A_TYPE_PROMPT = `
[A 타입 — 미니멀 텍스트형 응답 스타일]
A 타입은 시각 요소를 최소화하고, 텍스트 자체의 구조만으로 정보를 전달하는 UI 모달리티입니다.
사용자는 화면을 위→아래로 순차 읽으며 판단합니다.
이번 응답은 반드시 아래 규칙을 따르세요.

[본문(text) 작성 규칙]
- text 필드는 짧은 안내(1~2문장)만 담으세요. 본격 내용은 sections로 넘기세요.
- 여러 라벨드 정보(예: "1차 수정안", "2차 수정안", "변경 이유", "확인 필요 사항")는
  반드시 sections 배열에 라벨드 섹션으로 넣으세요. text에 인라인으로 풀어 쓰지 마세요.
- sections 형식:
    [
      { "label": "1차 수정안", "content": "..." },
      { "label": "변경 이유",  "content": "..." }
    ]
  - label: 짧은 소타이틀 (2~6글자).
  - content: 그 섹션의 본문. 단락 분리 필요하면 "\\n\\n" 사용.
- 후보 비교가 *카드 단위*로 명확할 때만 card를 사용. 단순 라벨드 텍스트는 sections로.
- 사용자가 다음에 누를 행동(예: "1번 표현 선택", "다른 표현 제안", "확정")은 chips로 제공.

[A 타입에서 피할 것]
- 한 단락에 여러 후보를 "1. ... 2. ..." 식으로 나열
- 머리표(- *), 마크다운 강조(**bold**, _italic_)
- 이모지를 강조 수단으로 사용 (chips·card.options의 emoji 필드는 예외)
- 시각 라벨/뱃지/애니메이션을 가정한 표현

[톤]
- 담백하고 짧게. 친절하지만 장식적이지 않게.
- 매번 응답에 추천 문장 + 판단 근거 + 다음 선택지 중 최소 3가지가 들어가되,
  그것들이 sections로 *시각적으로 분리*되어 있어야 합니다.
`;
