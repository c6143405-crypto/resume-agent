# 08. `app/classify-intent.ts` — 사용자 의도 분류기

**📁 파일 경로:** `app/classify-intent.ts`
**🔗 공용:** 모든 타입(A/B/C/D)이 같이 씀

---

## 뭐 하는 파일인가요?

사용자가 챗에 친 메시지를 보고 *"이게 어떤 의도지?"* 판단하는 함수.

8가지 의도 중 하나를 반환해요. AI는 이 결과를 받아서 그에 맞춰 응답해요.

---

## 8가지 의도

| Intent | 사용자가 한 말 예시 | AI가 어떻게 반응 |
|---|---|---|
| `ACCEPT` | "좋아요", "이걸로 할게요", "확정" | 장점 요약 + 확인 질문 |
| `REJECT` | "아닌 것 같아요", "기존 문장 유지" | 다른 후보 제안 |
| `MODIFY_TONE` | "덜 과장되게", "더 자연스럽게" | 톤 조정 |
| `MODIFY_CONTENT` | "내 경험이 아니에요", "이 내용 추가" | 경험 차이 확인 |
| `ASK_REASON` | "왜?", "이유", "근거가 뭐예요" | 판단 근거 설명 |
| `ASK_ALTERNATIVE` | "다른 안", "다시 추천", "다른 표현" | 대체 표현 2개 제안 |
| `UNCERTAIN` | "잘 모르겠어요", "고민돼요" | 선택지 줄여서 보여줌 |
| `LOW_CONFIDENCE` | "어려워요", "복잡해", "부담돼" | 쉬운 말로 설명 |

---

## 어떻게 작동하나

```tsx
const INTENT_KEYWORDS: Array<{ intent: UserIntent; keywords: string[] }> = [
  {
    intent: "ASK_REASON",
    keywords: ["왜 이렇게 판단", "왜", "이유", "근거"],
  },
  {
    intent: "REJECT",
    keywords: [
      "아닌 것 같아요",
      "싫어요",
      "빼주세요",
      "맞지 않아요",
      "기존 문장 유지",
      "내 경험이 아니",
    ],
  },
  ...
];

export function classifyUserIntent(userMessage: string): UserIntent {
  for (const { intent, keywords } of INTENT_KEYWORDS) {
    if (keywords.some((keyword) => userMessage.includes(keyword))) {
      return intent;
    }
  }
  return "UNCERTAIN";  ← 매칭 안 되면 기본값
}
```

**작동 순서:**

1. 사용자 메시지를 받음 (예: "이건 좀 과장된 거 같아요")
2. `INTENT_KEYWORDS` 배열을 *위에서 아래로* 순서대로 확인
3. 어떤 intent의 keyword 중 *하나라도 포함되면* 그 intent 반환
4. 아무것도 안 걸리면 `UNCERTAIN` 반환

**순서가 중요해요:**

배열 위쪽이 먼저 검사돼요. "왜"라는 짧은 단어가 다른 intent의 긴 문장에 포함되어도 ASK_REASON으로 먼저 잡히도록 위에 둔 거예요.

---

## 언제 만지나요?

✅ **사용자가 흔히 쓰는 표현이 분류 안 될 때**

예: 사용자가 "이거 별로네"라고 했는데 REJECT로 분류 안 됨 → "별로" 키워드 추가

```tsx
{
  intent: "REJECT",
  keywords: [
    "아닌 것 같아요",
    "싫어요",
    "별로",          ← 추가
    ...
  ],
},
```

✅ **새 intent 카테고리 만들고 싶을 때**

1. 먼저 `agent-state.ts`의 `UserIntent` 타입에 새 값 추가
2. 여기 `INTENT_KEYWORDS` 배열에 새 항목 추가
3. `api/chat/route.ts`의 SYSTEM_PROMPT에 그 intent에 대한 응답 규칙 추가

---

## 작동 방식 예시

```
사용자: "이건 좀 과장된 거 같아요"
   ↓
classify-intent.ts가 "과장" 키워드 검색
   ↓
"덜 과장" 매칭 (MODIFY_TONE의 keyword)
   ↓
intent: "MODIFY_TONE" 반환
   ↓
AI가 "톤 조정" 방향으로 응답 (route.ts의 SYSTEM_PROMPT 규칙대로)
```

---

## ❌ 만지면 안 되는 부분

- `import type { UserIntent } from "./agent-state";` 줄
- `export function classifyUserIntent(...)` 함수 시그니처 — 다른 곳에서 호출하므로
- `return "UNCERTAIN";` 기본값 — 변경 가능하지만 영향 큼

---

## 키워드 추가 팁

- **짧은 단어보다 *고유한 패턴*을 우선**
  - 나쁜 예: REJECT에 "이"를 추가 → 너무 흔해서 다른 메시지에도 잡힘
  - 좋은 예: "별로", "아닌 거 같"처럼 *그 의도에 특유한* 표현
- **사용자가 실제로 쓰는 말투를 관찰하고 추가**
  - 인터뷰나 테스트에서 자주 나오는 표현 메모해두고 한 번에 반영
- **시니어 사용자 어휘 고려**
  - 젊은 사람들이 쓰는 줄임말("ㄴㄴ", "별로ㅠ") 대신 정중한 표현을 우선
