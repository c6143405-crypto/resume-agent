# 06. `app/agent-state.ts` — 공용 타입 정의 (설계도)

**📁 파일 경로:** `app/agent-state.ts`
**🔗 공용:** 모든 타입(A/B/C/D)이 같이 씀

---

## 뭐 하는 파일인가요?

프로젝트 전체에서 *"이 데이터는 이런 모양이다"* 를 정의하는 **설계도** 같은 파일.

실제 데이터를 담는 게 아니라, *"Draft는 이런 필드를 가져야 한다"* 같은 *형식* 을 정의해요.
TypeScript의 `type`과 `interface` 만 있어서, 컴파일하면 실제로는 사라지는 파일이에요.

---

## 왜 필요한가요?

여러 파일이 같은 데이터를 주고받을 때, *모양이 안 맞으면* 오류가 나요.
이 파일이 *기준 형식*을 정해주니까, 다른 파일들은 이 기준대로만 데이터를 만들면 안전해요.

예시:
- `drafts.ts`가 초안 데이터를 만들 때 → 여기 정의된 `Draft` 형식대로
- `page.tsx`가 초안을 받아 쓸 때 → 같은 `Draft` 형식이라 안전
- 모양이 안 맞으면 Cursor가 *빨간 줄*로 경고해줌

---

## 안에 뭐가 있나

### 단순 값 타입들

```tsx
export type CurrentStep = "CM1" | "CM2";
export type PrototypeType = "A" | "B" | "C" | "D";
export type DecisionStatus =
  | "draft"
  | "selected"
  | "modified"
  | "confirmed"
  | "rejected";
```

- 이런 식으로 *허용된 값들만 모은 목록*

### 사용자 의도 8가지

```tsx
export type UserIntent =
  | "ACCEPT"
  | "REJECT"
  | "MODIFY_TONE"
  | "MODIFY_CONTENT"
  | "ASK_REASON"
  | "ASK_ALTERNATIVE"
  | "UNCERTAIN"
  | "LOW_CONFIDENCE";
```

이 8가지가 우리 프로젝트의 *의도 분류 카테고리* 전체. `classify-intent.ts`가 이걸 기준으로 사용자 발화를 분류해요.

### 초안 데이터 형식

```tsx
export interface Draft {
  draftId: string;
  draftTitle: string;
  draftContent: string;
  draftDirection: string;
  whyRecommended: string;
  caution: string;
  body: DraftBody;             ← 자세한 본문
  refinementTarget: DraftRefinementTarget;  ← CM2의 수정 예시
}
```

### 본문 구조

```tsx
export interface DraftBody {
  company: string;        ← "(주) A 의류"
  period: string;         ← "2012.03 ~ 현재"
  projectTitle: string;
  projectChip?: DraftChip;
  overview: string;
  goals: DraftBullet[];      ← 목표 bullets
  roleAndResults: DraftBullet[];  ← 역할 및 성과 bullets
}
```

### CM2 수정 대상

```tsx
export interface DraftRefinementTarget {
  originalSentence: string;   ← 기존 문장
  revisedSentence: string;    ← AI 수정안
  changeReason: string;       ← 변경 이유
}
```

### 전체 상태값

```tsx
export interface AgentState {
  currentStep: CurrentStep;
  prototypeType: PrototypeType;
  userIntent: UserIntent | null;
  decisionStatus: DecisionStatus;
  selectedDraft: SelectedDraft;
  draftOptions: Draft[];
}
```

### 기본값

```tsx
export const DEFAULT_AGENT_STATE: AgentState = {
  currentStep: "CM1",
  prototypeType: "A",
  userIntent: null,
  decisionStatus: "draft",
  selectedDraft: null,
  draftOptions: [],
};
```

각 페이지가 시작할 때 *이 기본값으로 초기화*돼요.

---

## 언제 만지나요?

✅ **새 필드 추가는 OK**
- 예: 모든 초안에 `rating` 필드를 추가하고 싶다 → `Draft` 인터페이스에 `rating?: number;` 추가

✅ **새 의도 카테고리 추가**
- `UserIntent`에 `"ASK_EXAMPLE"` 같은 거 추가

✅ **새 결정 상태 추가**
- `DecisionStatus`에 새 값

---

## ❌ 만지면 안 되는 부분

- **기존 필드 *이름*을 바꾸는 것** — 다른 파일이 다 깨져요
  예: `draftId` → `id`로 바꾸면, drafts.ts와 page.tsx 다 깨짐
- **기존 필드 *지우는 것*** — 그 필드를 쓰는 다른 파일들이 다 빨간 줄
- **`export` 키워드 빼기** — 다른 파일에서 import 못 함

---

## 작업 흐름

새 필드 추가하고 싶다 → 이 순서로:

1. `agent-state.ts`에서 형식 추가
2. `drafts.ts`에서 그 필드에 실제 값 채워주기
3. 사용하는 `page.tsx`에서 화면에 표시하기
4. (선택) `api/chat/route.ts`도 그 필드 활용

순서 안 지키면 Cursor가 빨간 줄로 잔뜩 경고해요. 한 번에 다 해야 안 깨져요.
