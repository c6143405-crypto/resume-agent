# 09. `app/api/chat/route.ts` — AI 호출 백엔드

**📁 파일 경로:** `app/api/chat/route.ts`
**🔗 공용:** 모든 타입(A/B/C/D)이 같이 씀

---

## 뭐 하는 파일인가요?

사용자가 챗에 글을 쓰면, *OpenAI(ChatGPT)한테 보내고 답을 받아오는* 서버 코드.

- 프론트엔드 (page.tsx) → 이 파일 → OpenAI → 응답 → 프론트엔드

이 파일은 *서버에서만 실행*되니까 OpenAI API 키를 안전하게 쓸 수 있어요.

---

## 큰 구조

```tsx
// 1. 공통 시스템 프롬프트 (모든 타입 공통)
const SYSTEM_PROMPT = `
당신은 시니어 재취업 준비자의 경험을...
[CM1 / CM2의 의미]
...
[userIntent별 응답 규칙]
...
`;

// 2. 응답 파싱 함수
function parseOpenAIResponse(content) {
  // OpenAI가 보낸 JSON을 { text, chips, card, sections } 형태로 정리
}

// 3. 메인 POST 핸들러
export async function POST(req) {
  // 페이지에서 보낸 데이터 받기
  const { messages, currentStep, userIntent, ..., typeStylePrompt } = body;

  // 시스템 메시지 구성
  const systemMessages = [
    공통 SYSTEM_PROMPT,
    타입별 typeStylePrompt,    ← A 페이지가 보낸 A_TYPE_PROMPT
    현재 컨텍스트 (currentStep, userIntent, selectedDraft),
  ];

  // OpenAI 호출
  const completion = await openai.chat.completions.create({
    model: 'gpt-4o-mini',
    messages: [...systemMessages, ...conversation],
  });

  // 응답 파싱해서 반환
  return NextResponse.json(parsed);
}
```

---

## SYSTEM_PROMPT (공통 — 가장 중요)

이 부분이 *AI의 정체성과 행동 규칙*을 정의해요. 모든 타입이 공유.

포함 내용:
- **Agent의 목적** — "판단 보조 Agent. 대신 완성하는 게 아니라 사용자가 선택·확정하게 도움."
- **CM1 / CM2의 의미** — 단계별 어떻게 행동할지
- **userIntent별 응답 규칙** — 8가지 intent에 어떻게 반응할지
- **금지 사항** — 없는 수치 만들지 마, 동의 없이 확정하지 마 등
- **JSON 출력 스키마** — `{ text, sections, chips, card }`

---

## 시스템 메시지 순서

```
1. SYSTEM_PROMPT (공통 정체성)
       ↓
2. typeStylePrompt (A 페이지가 보냄. A_TYPE_PROMPT 내용)
       ↓
3. 현재 턴 컨텍스트 (currentStep, userIntent, selectedDraft 등)
       ↓
4. 사용자-에이전트 대화 messages
       ↓
5. OpenAI 호출
```

A 타입의 응답 스타일(`A_TYPE_PROMPT`)은 *별도 system 메시지로* 끼워 넣어서, 공통과 분리해요.
B/C/D 타입 만들 때는 `B_TYPE_PROMPT`, `C_TYPE_PROMPT`, `D_TYPE_PROMPT`가 자동으로 들어옴.

---

## 언제 만지나요?

✅ **AI 모델 바꾸기**

```tsx
const MODEL = 'gpt-4o-mini';   ← 여기를 'gpt-4o', 'gpt-4-turbo' 등으로
```

비용/속도/품질 트레이드오프. mini가 가장 저렴.

✅ **공통 정체성·규칙 수정**

`SYSTEM_PROMPT` 백틱 안의 내용 자유 수정:
- "판단 보조 Agent" 정의 수정
- CM1/CM2 행동 원칙 수정
- userIntent별 응답 규칙 추가/수정
- 금지 사항 추가

✅ **응답 형식에 새 필드 추가**

- JSON 스키마에 새 필드 명시
- `parseOpenAIResponse` 함수에서 그 필드 파싱
- `ChatResponse` 타입에 추가
- page.tsx에서 받아서 화면에 표시

(이건 좀 큰 작업. 전체 파이프 다 손봐야 함)

---

## ❌ 만지면 안 되는 부분

- 상단 `import OpenAI from 'openai';` 등 import 영역
- `export async function POST(req: NextRequest)` 함수 시그니처
- 응답 형식 `{ text, chips, card, sections }` — 변경 시 모든 page.tsx 같이 수정 필요

---

## 환경 변수

```tsx
if (!process.env.OPENAI_API_KEY) {
  return NextResponse.json({ error: 'OPENAI_API_KEY 환경변수가 설정되지 않았습니다.' }, ...);
}
```

`.env.local`에 `OPENAI_API_KEY`가 없으면 즉시 에러 반환. *키 없으면 작동 안 함*.

---

## 서버 로그

```tsx
console.log('[api/chat] received state:', {
  currentStep,
  prototypeType,
  userIntent,
  decisionStatus,
  targetJob,
  draftOptionsCount: ...,
  selectedDraftId: ...,
  typeStylePromptLen: ...,
});
```

이 로그는 *터미널*(npm run dev 돌아가는 곳)에 찍혀요. 디버깅할 때 메시지마다 어떤 상태가 들어왔는지 확인 가능.

---

## FALLBACK_RESPONSE

```tsx
const FALLBACK_RESPONSE: ChatResponse = {
  text: '좋아요. 말씀해주신 내용을 바탕으로...',
  chips: ['표현을 더 간결하게', ...],
  card: null,
  sections: [],
};
```

OpenAI가 잘못된 JSON을 보내면 *이 기본 응답이 대신 나와요*. 사용자가 에러 화면 안 봐도 되게 안전망.
