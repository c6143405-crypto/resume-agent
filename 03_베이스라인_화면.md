# ❌❌❌ 절대 만지면 안 됨 ❌❌❌

> 이 파일은 **A/B/C/D 타입의 비교 기준점**이에요.
> **손대면 연구 비교 자체가 의미를 잃습니다.**
> 정말 바꿔야 할 이유가 있다면 *팀과 먼저 상의* 하세요.

---

# 03. 베이스라인 화면

**📍 URL:** `localhost:3000`
**📁 실제 파일:** `app/page.tsx`

---

## 뭐 하는 파일인가요?

원본 디자인의 메인 화면. 우리가 4가지 타입을 만들기 *전*의 모습이에요.

A/B/C/D 타입이 *이 파일에서 출발*해서 변형되는 거라, **원본이 살아있어야 비교가 의미 있어요.**

---

## ❌ 만지지 마세요

이 파일은 **비교 기준점**이에요. 손대면:
- A/B/C/D 타입과 비교할 때 *원본이 뭐였는지* 알 수 없어짐
- 연구 결과의 일관성이 깨짐

만약 *이 파일의 내용 자체*를 바꾸고 싶다면, 그건 사실 모든 타입의 *공통 부분*을 바꾸는 거예요. 그땐 팀과 먼저 상의하세요.

---

## 안에 뭐가 있는지

원본 프로토타입의 전체 코드가 한 파일에 들어 있어요:

- **상태 관리** — `useState`로 화면 흐름·메시지·선택 상태 관리
- **화면 흐름:**
  1. `selectRole` — 직무 선택 (옵션 A 회계담당자 / 옵션 B 재무관리자)
  2. `loadingDraft` — 로딩 안내 (5초)
  3. `draftReady` — 2개 샘플 경력기술서 리스트
  4. `chat` — 챗 흐름 (STAGES 시스템으로 응답)
- **컴포넌트:**
  - `BottomSheet` — 시트 화면 (하드코딩된 회계팀 콘텐츠)
  - `ResumeRow` — 샘플 리스트 한 줄
  - `AiOrbLogo` — 둥근 로고
  - `Chip` — 칩 (gray / blue / purple)
  - `StatusBar` — 상단 시계·배터리

---

## 화면 흐름

```
[직무 선택]
   ↓ (옵션 A 또는 B 선택)
[5초 로딩 안내]
   ↓
[2개 샘플 경력기술서 리스트]
   ↓ (샘플 1/2 고치고 싶다 클릭)
[챗 화면 — STAGES 흐름]
   ↓
사용자 ↔ AI 대화
```

---

## A 타입이 이 파일에서 무엇을 바꿨는지 (참고용)

- 직무 선택 화면 *건너뜀* (초기 flowStep을 `loadingDraft`로)
- 로딩 시간 5초 → 3초
- 2개 샘플 → 3개 초안 (라디오 + 행 탭→시트)
- BottomSheet 본문: 하드코딩 콘텐츠 → 초안별 동적 콘텐츠
- BottomSheet 상단: "김효원님이..." 인트로 → "근거 보기" 아코디언
- 챗 첫 에이전트 메시지: STAGES → CM2 수정 카드
- 챗 입력창: CM1에서 숨김

같은 패턴으로 B/C/D 타입도 *이 베이스라인에서 출발*해서 각자의 원칙대로 변형하면 돼요.
