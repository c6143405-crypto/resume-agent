# 01. README — 프로젝트 소개와 시작하기

> 처음 프로젝트를 받았을 때 *맨 처음* 보는 파일이에요. 한 번만 따라하면 끝.

---

## 이게 무슨 프로젝트인가요?

시니어 재취업 분들이 자기 경력을 글로 옮길 때 AI가 어떻게 도와주면 좋을지를
*디자인 4가지로 비교 실험*하는 연구용 프로토타입.

같은 기능을 4가지 다른 화면 스타일(A/B/C/D)로 만들어서, 어떤 게 더 좋은지
비교 테스트하는 게 목적이에요.

---

## 5개의 화면

| 주소 | 뭘 보여주는지 |
|---|---|
| `localhost:3000`   | **베이스라인** — 원본 디자인 (기준점) |
| `localhost:3000/A` | **A 타입** — 미니멀 텍스트 스타일 (완성) |
| `localhost:3000/B` | **B 타입** — 시각 라벨 스타일 (만들 예정) |
| `localhost:3000/C` | **C 타입** — 카드 묶음 스타일 (만들 예정) |
| `localhost:3000/D` | **D 타입** — 동적 인터랙션 스타일 (만들 예정) |

---

## 시작하기 — 한 번만 설치

### ① Node.js 설치
- https://nodejs.org 에서 **LTS** 큰 초록 버튼 → 다운로드 → 그냥 *다음, 다음*
- *"이 프로젝트를 켤 수 있게 해주는 엔진"* 정도로 생각하세요.

### ② Cursor에서 프로젝트 가져오기
- Cursor 실행 → **Clone Repository**
- 우리 GitHub 주소 붙여넣기 → 컴퓨터에 저장할 위치 선택 → 완료

### ③ 부품 깔기
- Cursor 안에서 **Terminal → New Terminal**
- 검은 창에 입력:
  ```
  npm install
  ```
- 2~3분 기다리면 끝.

### ④ 비밀 키 넣기

> 🔒 **왜 따로 하나요?**
> OpenAI API는 *돈이 드는 서비스*예요. 키가 GitHub에 올라가면 누구나 가져다 쓸 수 있고, 요금은 우리한테 청구돼요. 그래서 키는 *각자 본인 컴퓨터에만* 저장해요.

- Cursor 왼쪽 파일 목록에서 *맨 위 폴더* 우클릭 → **New File**
- 파일 이름: `.env.local`  ← 점부터 시작!
- 안에 두 줄:
  ```
  OPENAI_API_KEY=여기에_팀_리더가_준_키
  NEXT_PUBLIC_USE_AI=true
  ```
- 키 값은 팀 리더한테 카톡/메신저로 따로 받으세요.

### ⑤ 켜기

- 터미널에서:
  ```
  npm run dev
  ```
- `Local: http://localhost:3000` 같은 글자 뜨면 성공.
- 브라우저에서 `http://localhost:3000` 접속.

> 💡 **터미널은 끄지 마세요.** 이 검은 창이 *프로젝트의 엔진*이에요. 끄면 브라우저 화면도 멈춰요.

---

## 성공 신호

- `localhost:3000` → 베이스라인 화면 보임 (직무 선택 → 로딩 → 샘플 두 줄)
- `localhost:3000/A` → A 타입 화면 보임 (3개 초안 라디오 + 시트)
- 챗에서 메시지 보내면 AI 응답

---

## 다음에 볼 문서

- **매일 작업할 때:** [`02_GUIDE.md`](./02_GUIDE.md)
- **각 파일이 뭔지 알고 싶을 때:** 03~10번 파일들
