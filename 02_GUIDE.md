# 02. GUIDE — 작업 흐름

> 매일 작업할 때 보는 페이지. *어디 만지는지 / 어떻게 저장하는지 / 문제 생겼을 때*.

---

## 파일 구조 한눈에

```
resume-agent/
├── app/
│   ├── page.tsx              ← /  (베이스라인)        → 03번 문서
│   ├── A/
│   │   ├── page.tsx              /A (A 타입 화면)     → 04번 문서
│   │   └── style-prompt.ts       A 타입 AI 응답 스타일 → 05번 문서
│   ├── B/, C/, D/            ← 작업 시작 시 새로 만들기
│   ├── agent-state.ts        ← 공용 타입 정의          → 06번 문서
│   ├── drafts.ts             ← 공용 초안 데이터        → 07번 문서
│   ├── classify-intent.ts    ← 공용 의도 분류기        → 08번 문서
│   └── api/chat/route.ts     ← 공용 AI 백엔드          → 09번 문서
├── (자동 관리 파일들)                                  → 10번 문서
└── .env.local                ← 본인 컴퓨터에만 있음
```

---

## 어디를 만져야 하는지

| 하려는 일 | 만지는 파일 | 자세한 설명 |
|---|---|---|
| A 타입 *화면 모양* 수정 | `app/A/page.tsx` | [04번](./04_A타입_화면.md) |
| A 타입 *AI 응답 스타일* 수정 | `app/A/style-prompt.ts` | [05번](./05_A타입_AI응답.md) |
| 새 타입 (B/C/D) 만들기 | `app/B/page.tsx` + `app/B/style-prompt.ts` 생성 | 04, 05번 참고 |
| 3개 초안 *내용* 바꾸기 | `app/drafts.ts` | [07번](./07_초안_데이터.md) |
| AI *전체적 성격·규칙* 바꾸기 | `app/api/chat/route.ts` | [09번](./09_AI_백엔드.md) |
| 사용자 *의도 분류 키워드* | `app/classify-intent.ts` | [08번](./08_의도_분류.md) |
| 모든 타입 *공유 데이터 구조* | `app/agent-state.ts` | [06번](./06_타입_정의.md) |

### 절대 만지면 안 되는 곳

- `node_modules/`, `.next/` 폴더 — *자동 관리*. 사람이 만지면 망가짐
- `package.json`, `package-lock.json` — 부품 목록. 직접 수정 X
- `tsconfig.json`, `next.config.ts`, `postcss.config.mjs` — 프로젝트 설정. 그냥 두기

---

## Cursor에서 저장하고 GitHub에 올리기

### 작업 시작할 때 — Pull 먼저

다른 팀원이 먼저 작업했을 수 있어요. 시작 전에 최신 코드 받아두기.

1. 왼쪽 사이드바 **Y자 아이콘** (Source Control) 클릭
2. 위쪽 `...` 메뉴 → **Pull**

### 작업 중간에 — Commit (내 컴퓨터에 저장)

1. 파일 수정 후 `Cmd + S`로 저장
2. Source Control 패널에서 변경된 파일 옆 `+` 클릭
3. 위쪽 입력 칸에 한 줄 메모 (예: `A 타입 버튼 색 수정`)
4. 체크 표시(✓) 클릭

### 다 끝나면 — Push (GitHub에 올리기)

1. 위쪽 `...` → **Push**
2. 다른 팀원도 볼 수 있게 됨

> 💡 **Commit = 내 컴퓨터에 저장 / Push = GitHub에 올리기**. 두 단계예요.

---

## 자주 묻는 것 (FAQ)

**Q. 코드 고쳤는데 브라우저에 안 바뀌어요**
A. 터미널 창이 꺼졌을 수 있어요. `npm run dev` 다시 실행. 안 되면 브라우저 새로고침.

**Q. `/A` 화면이 텅 비어 있어요**
A. 99%는 `.env.local`이 없거나 키가 잘못된 거. [01번](./01_README.md) ④번 단계 다시 확인.

**Q. 터미널에 빨간 글씨가 떠요**
A. 빨간 글씨를 *복사해서 팀 리더한테* 보내세요.

**Q. 실수로 망가뜨렸어요**
A. 괜찮아요. GitHub에 옛 버전들이 다 저장되어 있어요. 당황 말고 알려주세요.

**Q. 노트북 껐다 켰는데 어떻게 시작?**
A.
1. Cursor 열기 (프로젝트 자동으로 열림)
2. Terminal → New Terminal
3. `npm run dev`
4. 브라우저로 `localhost:3000`

설치는 다시 안 해도 돼요. 한 번 한 건 컴퓨터에 남아 있어요.

**Q. 새 타입(B/C/D)을 시작하려면?**
A.
1. `app/A/` 폴더 통째로 복사 → `app/B/`로 이름 바꾸기
2. `app/B/style-prompt.ts` 안의 `A_TYPE_PROMPT` → `B_TYPE_PROMPT`로 이름 변경
3. `app/B/page.tsx` 안의 import도 `A_TYPE_PROMPT` → `B_TYPE_PROMPT`로 수정
4. B 타입 원칙에 맞게 UI/프롬프트 수정

---

## 더 자세한 정보

각 파일이 *정확히* 뭘 하는지 궁금하면:
- [03번 — 베이스라인 화면](./03_베이스라인_화면.md)
- [04번 — A 타입 화면](./04_A타입_화면.md)
- [05번 — A 타입 AI 응답 스타일](./05_A타입_AI응답.md)
- [06번 — 공용 타입 정의](./06_타입_정의.md)
- [07번 — 초안 데이터](./07_초안_데이터.md)
- [08번 — 의도 분류기](./08_의도_분류.md)
- [09번 — AI 백엔드](./09_AI_백엔드.md)
- [10번 — 자동 관리 파일들](./10_자동관리_파일들.md)
- [11번 — 데이터 흐름과 FAQ](./11_데이터_흐름과_FAQ.md)
