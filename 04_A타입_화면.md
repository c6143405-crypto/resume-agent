# 04. `app/A/page.tsx` — A 타입 화면

**📍 URL:** `localhost:3000/A`
**📁 파일 경로:** `app/A/page.tsx`

---

## 뭐 하는 파일인가요?

A 타입(미니멀 텍스트형)의 화면 코드.
사용자가 `/A` 주소에서 보는 *모든 화면*이 여기 들어 있어요.

---

## A 타입의 디자인 원칙

- **텍스트 위주**: 시각 라벨/뱃지/애니메이션 최소화
- **위→아래 순차 읽기**: 정보를 선형으로 배치
- **최소 시각 요소**: 카드/그라데이션/복잡한 인터랙션 없음
- **단순 라디오 + 텍스트 버튼**: 사용자가 한눈에 무엇이 동작인지 알게

---

## 화면 흐름

```
[5초 로딩 안내]  ← 직무 선택 화면은 임시 숨김
   ↓
[CM1 화면]
  - 안내 문구 4줄
  - 3개 초안 리스트 (라디오 + 제목 + chevron)
  - 행 탭 → 바텀시트 (전체 내용)
  - 하단 "이 초안 선택하기" 버튼
   ↓ (확정)
[CM2 화면 = 챗 진입]
  - 안내 메시지 + 수정 카드
    - 선택한 초안 / 기존 문장 / AI 수정안 / 변경 이유
    - 3 버튼 (수정안 적용 / 기존 유지 / 다시 수정)
  - 챗 입력창 활성화
   ↓
사용자 ↔ AI 대화 (라벨드 섹션으로 응답)
```

---

## 언제 만지나요?

✅ **자주 만질 곳**
- 라디오 색 바꾸기 (예: `#171719` → 다른 색)
- 버튼 모양·위치 조정
- CM2 수정 카드의 레이아웃
- 사용자 메시지 / 에이전트 응답의 글자 크기·여백

✅ **가끔 만질 곳**
- 챗 흐름 분기 (예: 특정 버튼 누르면 다른 액션)
- 새 상태값 추가

---

## ❌ 만지면 안 되는 부분

### 상단 import 영역

```tsx
import { useEffect, useRef, useState } from "react";
import { FileText, X } from "lucide-react";
import type { ... } from "../agent-state";   ← 경로 바꾸면 깨짐
import { ... } from "../drafts";
import { ... } from "../classify-intent";
import { A_TYPE_PROMPT } from "./style-prompt";
```

이 줄들을 *지우거나 경로 변경*하면 페이지가 작동 안 함.

### 함수 이름

```tsx
const commitCm1Selection = () => { ... }
const handleRefinementApply = () => { ... }
const openBottomSheetWith = (draft) => { ... }
```

이런 함수 이름들을 *바꾸면* 다른 곳에서 호출하는 부분이 다 깨져요.
함수 *내용*은 자유롭게 수정 가능, *이름*만 그대로.

---

## 안에 뭐가 있나 (대략적인 영역 구분)

| 줄 번호 (대략) | 영역 |
|---|---|
| 1~17 | import 영역 |
| 19~30 | 환경 변수, 옵션 데이터 |
| 50~290 | 작은 컴포넌트들 (Chip, ResumeRow, StatusBar, BottomSheet) |
| 290~470 | BottomSheet 내부 (회사/프로젝트/개요/목표/역할 렌더) |
| 470~620 | 상태값 (useState 묶음) + useEffect |
| 620~1010 | 핸들러 함수들 (sendMessage, commit, refinement 등) |
| 1010 이후 | 메인 `return` (실제 JSX 화면) |
| 1010~1280 | 화면 분기 (loadingDraft / draftReady / chat) |
| 1280~1700 | 챗 영역 (메시지 렌더, 수정 카드, 라벨드 섹션, 칩) |
| 1700~ | 하단 버튼·입력창 |

---

## 주요 상태값

| 변수 | 뜻 |
|---|---|
| `view` | "home" 또는 "chat" — 화면 큰 영역 |
| `flowStep` | "selectRole" / "loadingDraft" / "draftReady" — CM1 진행 단계 |
| `cm1Candidate` | CM1에서 라디오로 골라둔 후보 초안 |
| `selectedDraft` | CM2 진입 시 확정된 초안 |
| `messages` | 챗에 쌓인 메시지 배열 |
| `currentStep` | "CM1" 또는 "CM2" — 단계 표시 |
| `userIntent` | 마지막 사용자 발화의 의도 (ACCEPT, REJECT 등) |
| `refinementCardOutcome` | "apply" / "keep" / "retry" / null — 수정 카드 결정 결과 |

---

## 자주 수정할 만한 위치 찾는 법

Cursor에서 파일 열고 `Cmd + F`로 검색:

- 라디오 디자인 → `isPicked` 검색
- 근거 보기 아코디언 → `isRationaleOpen` 검색
- CM2 수정 카드 → `refinementCard &&` 검색
- 챗 메시지 렌더 → `chatMessage.text` 검색
- 안내 문구 4줄 → `아래 ` 또는 `초안을 준비` 검색
