# ❌❌❌ 절대 만지면 안 됨 ❌❌❌

> 이 문서에 나오는 파일들은 **프로젝트가 자동으로 관리**해요.
> **사람이 직접 손대면 프로젝트가 망가질 수 있습니다.**
> *어떤 파일이 자동 관리되는지 알고 싶을 때만* 참고용으로 보세요.

---

# 10. 자동 관리 파일들

---

## `app/layout.tsx` — 전역 레이아웃

**뭐 하는 파일?**
모든 페이지 위에 공통으로 적용되는 *바깥 껍데기*. 메타 정보, 폰트, 전체 배경 등.

**언제 만지나?**
- 거의 안 만져요.
- 굳이 만지면: 전역 폰트 바꾸기, 페이지 메타 설명 수정 정도.

**예시 내용:**
```tsx
export const metadata = {
  title: "경력기술서 에이전트",
  description: "...",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  );
}
```

---

## `app/globals.css` — 전역 CSS

**뭐 하는 파일?**
모든 화면에 적용되는 *전역 CSS*. 기본 색상 변수, 폰트 임포트 등.

**언제 만지나?**
- 거의 안 만져요.
- 굳이 만지면: 디자인 시스템 색 통째로 바꿀 때.

> ⚠️ 여기 손대면 전체 화면이 영향받아요. 한 곳만 손대도 4개 타입 다 영향.

---

## `.env.local` — 비밀 키

**뭐 하는 파일?**
OpenAI API 키 같은 *비밀 설정*. 본인 컴퓨터에만 있어요.

**언제 만지나?**
- 키가 바뀌었을 때 (팀 리더가 새 키 보내주면 교체)
- AI 모드 끄고 STAGES 모드(미리 정의된 응답)로 테스트하고 싶을 때:
  ```
  NEXT_PUBLIC_USE_AI=false
  ```

**🔒 주의**
- 절대 GitHub에 올리지 말기
- `.gitignore`에 등록되어 있어서 *자동으로 안 올라가긴 함*
- 다른 사람과 키 공유할 때는 *비밀 채널*(카톡 1:1, 슬랙 DM 등)로

---

## `package.json` — 부품 목록

**뭐 하는 파일?**
프로젝트가 *어떤 라이브러리(부품)를 쓰는지* 적힌 목록.

**언제 만지나?**
- 직접 만지지 마세요.
- 새 라이브러리 추가할 때는 `npm install 라이브러리이름` 명령어가 *자동으로* 이 파일을 고쳐줘요.

---

## `package-lock.json` — 부품 버전 잠금

**뭐 하는 파일?**
`package.json`에 적힌 부품들의 *정확한 버전*을 기록. 팀원들이 *똑같은 환경*을 쓰도록 보장.

**언제 만지나?**
- **절대 손대지 마세요.** 자동 관리 파일.

---

## `next.config.ts` — Next.js 설정

**뭐 하는 파일?**
Next.js 프레임워크 설정. 어떻게 빌드할지, 어떤 기능을 켤지.

**언제 만지나?**
- 거의 안 만져요.
- 굳이 만지면: 외부 이미지 도메인 허용, 환경 변수 노출 설정 등.

---

## `tsconfig.json` — TypeScript 설정

**뭐 하는 파일?**
TypeScript 컴파일러 설정. 어떤 문법 규칙을 쓸지.

**언제 만지나?**
- 거의 안 만져요.
- 굳이 만지면: 경로 별칭(alias) 추가, strict 모드 조정 등.

---

## `postcss.config.mjs` — CSS 처리 설정

**뭐 하는 파일?**
Tailwind CSS가 작동하도록 하는 설정.

**언제 만지나?**
- **절대 안 만져요.** Tailwind 자체 설정 변경할 때만 (그것도 잘 없음).

---

## `next-env.d.ts` — Next.js 자동 생성

**뭐 하는 파일?**
Next.js가 자동으로 만드는 TypeScript 타입 파일.

**언제 만지나?**
- **만지지 마세요.** 자동 생성 파일.

---

## `.gitignore` — Git 제외 목록

**뭐 하는 파일?**
*Git에 안 올라갈 파일들*의 목록. `.env.local`, `node_modules/` 등이 여기 적혀 있어서 GitHub에 안 올라감.

**언제 만지나?**
- 거의 안 만져요.
- 굳이 만지면: 새로 *비밀로 두고 싶은 파일* 추가 (예: 로컬 메모 `notes.md` 추가하고 싶다)

---

## `node_modules/` 폴더

**뭐 하는 폴더?**
`npm install`로 다운받은 *모든 부품들*이 들어있는 폴더. 수천 개 파일.

**언제 만지나?**
- **절대 만지지 마세요.** 자동 관리.
- 폴더 통째로 *삭제해도 됨* — `npm install` 다시 하면 복구됨.
- GitHub에는 안 올라감 (`.gitignore`에 등록).

---

## `.next/` 폴더

**뭐 하는 폴더?**
`npm run dev`나 `npm run build` 할 때 *자동 생성*되는 빌드 결과물.

**언제 만지나?**
- **만지지 마세요.** 자동 관리.
- 폴더 통째로 *삭제해도 됨* — 다음 실행 때 자동 재생성.

---

## 정리

| 파일/폴더 | 만지나? | 비고 |
|---|---|---|
| `app/layout.tsx` | 가끔 | 전역 폰트·메타 |
| `app/globals.css` | 가끔 | 전역 색상 |
| `.env.local` | 가끔 | 키 바뀔 때 |
| `package.json` | ❌ npm이 관리 | 직접 X |
| `package-lock.json` | ❌ npm이 관리 | 직접 X |
| `next.config.ts` | ❌ 거의 X | 설정 변경 시만 |
| `tsconfig.json` | ❌ 거의 X | 설정 변경 시만 |
| `postcss.config.mjs` | ❌ X | 변경할 일 없음 |
| `next-env.d.ts` | ❌ X | 자동 생성 |
| `.gitignore` | 가끔 | 새 비밀 파일 추가 시 |
| `node_modules/` | ❌ X | npm이 관리 |
| `.next/` | ❌ X | 빌드 결과물 |
